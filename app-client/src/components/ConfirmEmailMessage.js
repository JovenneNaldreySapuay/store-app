import React from 'react';
import { Message } from 'semantic-ui-react';
		
const ConfirmEmailMessage = () => (
	<Message info>
		<Message.Header>Please confirm your email to unlock full features.</Message.Header>
	</Message>
);

export default ConfirmEmailMessage;
